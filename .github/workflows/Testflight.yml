name: Deploying to Testflight

on:
  release:
    types: [prereleased]
jobs:
  CD:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@master
    
    - name: Installing Cocoapods
      run: |
       pod install
       pod update
 
    - name: Running Tests
      run: fastlane beta
